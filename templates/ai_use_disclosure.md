# AI-Use Disclosure & Authorship Integrity

Project: <short name>
Audit mode: open / pre-review / final
Updated at (Beijing): <YYYY-MM-DD HH:MM>
Owner: <accountable human>

Purpose: record every substantive use of a generative-AI system in this paper,
how a human verified it, and who is answerable for it — then render the
venue-shaped disclosure statement from that record. Contract and blocking rules:
[`references/ai-use-disclosure.md`](../references/ai-use-disclosure.md).

> Stage 7 removes the AI *accent*. It must never remove the AI *disclosure*.

## 1. Venue Policy

- policy_family: <elsevier | springer-nature | wiley-sage-tf | aea-econ | cn-journal | other>
- policy_source: <URL or citation; required when policy_family is `other`>
- target_venue: <journal / 学位论文单位>
- statement_placement: <e.g. titled section before references / Methods / Acknowledgements / 致谢>
- ai_as_author: no
- authors_take_responsibility: yes

## 2. AI-Use Ledger

One row per AI-assisted artifact, appended at the end of the stage that produced
it. `Human verification` vocabulary: `rerun`, `recomputed`, `source-checked`,
`read-and-edited`, `spot-checked (<rate>)`, `unverified`.

| Stage | Category | Tool / model | Used for | Human verification | Accountable | Disclose |
|---|---|---|---|---|---|---|
| 1L | literature | <tool + version> | <screened N candidate papers into the corpus> | source-checked | <name> | yes |
| 3 | code | <tool + version> | <wrote the estimation script> | rerun | <name> | yes |
| 5 | text | <tool + version> | <drafted Sections 1-6> | read-and-edited | <name> | yes |
| 7 | text | <tool + version> | <rewrote Sections 1-6 for readability and non-formulaic phrasing> | read-and-edited | <name> | yes |

Category vocabulary: `literature` / `code` / `analysis` / `text` / `translation`
/ `figure` / `data`.

## 3. Prohibited-Use Screen

| # | Check | Verdict |
|---|---|---|
| B1 | No AI system appears as an author or co-author | pending |
| B2 | No data value, series, observation or result image was generated by AI | pending |
| B3 | No `code` / `analysis` / `data` / `literature` row is `unverified` | pending |
| B4 | The de-AIGC stage (Stage 7) has its own ledger row | pending |
| B5 | Every `Disclose: yes` category appears in the rendered statement | pending |
| B7 | AI-suggested references were resolved in `00_meta/citation_integrity_log.md` | pending |
| B8 | Every disclosed row names an accountable human | pending |

## 4. Rendered Statement

Venue-shaped text that ships with the manuscript. Name the tool and version, the
purpose, the human review, and the responsibility clause.

> <rendered statement>

## 5. Deferred / Not Disclosed

Rows deliberately marked `Disclose: no` and why they are trivial under the
policy (spell-check, reference formatting, no substantive contribution).

| Stage | What | Why it is trivial |
|---|---|---|
| <n> | <e.g. grammar-only correction pass> | <no substantive content contribution> |

## 6. State Sync

Write back to `workflow_state.json.ai_disclosure`:

- `status`: `pending` / `pass` / `not_pass`
- `disclosure_file`: `00_meta/ai_use_disclosure.md`
- `policy_family`, `statement_placement`
- `ledger_rows`, `disclosed_rows`, `unverified_rows`
- `blocking_findings`: []
- `last_audit`: `<YYYY-MM-DD HH:MM>`

Then run:

```bash
python3 scripts/check_ai_disclosure.py <workspace>          # per-stage
python3 scripts/check_ai_disclosure.py <workspace> --final  # Stage 9
python3 scripts/check_workspace_gates.py <workspace>        # cross-gate ordering
```
